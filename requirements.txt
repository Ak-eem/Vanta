# VANTA pinned dependency set
# Install: pip install -r requirements.txt
# Then:    playwright install chromium

# ── Core ──────────────────────────────────────────────────────
flask==3.1.1
flask-socketio==5.5.1
groq==0.18.0
python-dotenv==1.1.1

# ── Web automation (orchestrator + Google search) ─────────────
playwright==1.54.0
playwright-stealth==1.0.6

# ── Watcher daemon ────────────────────────────────────────────
watchdog==6.0.0
feedparser==6.0.0

# ── Utilities ─────────────────────────────────────────────────
# eventlet is intentionally not included: server.py uses async_mode="threading"
# and the project no longer depends on eventlet.
